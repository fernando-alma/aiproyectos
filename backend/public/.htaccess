<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On
    
    # Base específica para la carpeta pública del backend
    RewriteBase /backend/public/

    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>